const Wishlist = ()=>{
    //useState [wish,setWish] = ([])
    //useState [products,setProducts] = ([])
    //check if better to take all products from DB then use Array.find on wish Array, because then you take data once and can filter !
    //useEffect - get wishlist from DB
    //useEffect load product data from DB (findProductById)

    return(
        //products.map.... render each product
        <h1>IMPLEMENT by saar !</h1>
    )
}

export default Wishlist;